
# Web-ларёк: Бэкенд

REST API для интернет-магазина «Web-ларёк». Сервер на Express + TypeScript с MongoDB: управление товарами, оформление заказов, JWT-авторизация с refresh-токенами, загрузка изображений, валидация данных через celebrate/joi, централизованная обработка ошибок и логирование.

## Технологии

- Node.js, Express, TypeScript
- MongoDB, Mongoose (схемы, валидация, триггеры)
- JWT-авторизация (access + refresh токены, httpOnly cookies)
- celebrate + joi (валидация запросов)
- multer (загрузка файлов)
- bcrypt (хеширование паролей)
- express-winston (логирование запросов и ошибок)
- CORS
- ESLint (Airbnb стайлгайд)

## Запуск

```bash
# Установка зависимостей
npm install

# Разработка с хот-релоадом
npm run dev

# Сборка
npm run build

# Запуск
npm run start
```

## API

### Товары

| Метод | Роут | Описание | Ошибки |
|-------|------|----------|--------|
| GET | `/product` | Получить все товары | 500 |
| POST | `/product` | Создать товар | 400, 401, 409, 500 |
| PATCH | `/product/:productId` | Обновить товар | 400, 401, 404, 409, 500 |
| DELETE | `/product/:productId` | Удалить товар | 400, 401, 404, 500 |

### Заказы

| Метод | Роут | Описание | Ошибки |
|-------|------|----------|--------|
| POST | `/order` | Оформить заказ | 400, 500 |

### Авторизация

| Метод | Роут | Описание | Ошибки |
|-------|------|----------|--------|
| POST | `/auth/login` | Авторизация | 401, 500 |
| POST | `/auth/register` | Регистрация | 400, 409, 500 |
| GET | `/auth/token` | Обновить токены | 401, 404, 500 |
| GET | `/auth/logout` | Выход | 401, 404, 500 |
| GET | `/auth/user` | Текущий пользователь | 401, 404, 500 |

### Загрузка файлов

| Метод | Роут | Описание | Ошибки |
|-------|------|----------|--------|
| POST | `/upload` | Загрузка изображения | 401, 500 |
